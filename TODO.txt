
==== PyPluginSp ====
-> Logging output per module.
-> Communicate on named pipes instead of stdio. STARTED
    -- Create native pipe module/class for embedded. STARTED
-> Modules sharing interpreters.
-> Track measure count per subprocess and terminate unneeded interpreters instances. DONE
-> Finalize embedded Python interpreter if all measures are unloaded.